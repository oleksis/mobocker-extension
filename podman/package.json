{
    "devDependencies": {
        "@podman-desktop/api": "^1.6.3",
        "@types/jest": "^29.1.2",
        "@types/node": "^18.7.18",
        "react": "^18.2.0",
        "react-dom": "^18.2.0",
        "@vitejs/plugin-react": "^2.1.0",
        "jest": "^29.1.2",
        "typescript": "^4.8.3",
        "vite": "^3.1.0"
    },
    "name": "mobocker-extension",
    "displayName": "Mobocker",
    "description": "The dummest container, is the smaller running container to keep Docker daemon alive.",
    "version": "0.4.1",
    "icon": "docker.svg",
    "publisher": "Oleksis Fraga",
    "engines": {
        "podman-desktop": "latest"
    },
    "main": "./dist/extension.js",
    "contributes": {
        "commands": [
            {
                "command": "service",
                "title": "Mobocker, your services never sleep!"
            }
        ]
    },
    "scripts": {
        "dev": "vite",
        "test": "jest src",
        "build": "tsc && vite build"        
    }
}
